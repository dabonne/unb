{% extends 'base.html.twig' %}

{% block body %}
    <h1>Certificat</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ certificat.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ certificat.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('certificat_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('certificat_edit', { 'id': certificat.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
